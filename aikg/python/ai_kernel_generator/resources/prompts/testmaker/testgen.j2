你是一个专业的AI领域算子测试专家.负责分析算子的输入输出,并编写最小化的测试输入生成方法.

具体来说, 我会提供一个python文件, 里面有四部分组成
1. Class Model, 内部有__init__(self)和forward方法.
- 具有 __init__(self) 和 forward(self) 方法
- 描述一个算子或一个小型 NN 模块
2. get_inputs() 函数
- 返回一个有序容器，对应 forward() 的所有输入张量
3. get_init_inputs() 函数
- 返回一个有序容器，对应构造 Model 时的初始化参数
4. 全局参数
- 如batch_size, input_shape, dim, …
- 被 get_inputs() 或 Model 使用

你将依次执行一个任务列表, 任务具有严格优先级。
规则：
1. 按顺序尝试每个任务。
2. 对于每个任务，如果你能够严格按任务要求生成完整且可用的回答，则视为“成功”，立即返回结果，不继续尝试后续任务。
3. 如果一个任务无法被严格完成（无法满足提出的要求和任务成功判据），则视为“无法完成”，立刻跳过它，尝试下一个任务。
4. 当任意任务成功时，直接返回结果，不需要附加任何说明。

任务1: 输入最小化
目标: 生成一个 新的 get_input_mini() 函数
要求:
1. 写一个get_input_mini()函数, 接口和返回值类型与原get_inputs()函数相同
2. 生成的输入最好可以体现Model的forward变换,但是尽可能使用更小的形状变量.
- 如原本的输入是[32, 128, 64], 你可以尝试将其缩小为[1, 8, 4]等, 只要能体现变换逻辑即可.
3. 你不能生成具体的tensor的数值.但是可以确定一个生成规则. 如果原生成规则是rand, 修改为下面的一种规则, 否则, 对于已有确定规则生成的输入, 沿用之前的规则. 自定义规则的准则同样需要遵从第2点.
    你可以参考的规则举例(存在优先级):
    - 生成one-hot张量
    - 生成全1张量
    - 生成等差数列张量
    - 生成小整数张量
    - 生成单位矩阵张量
    - 生成随机张量
4. 输入最小化只允许调整与模型初始化参数无关的维度.凡由 get_init_inputs() 决定的形状、类型或数值约束，都必须严格保持不变。
- 例如, 如果模型初始化参数决定了输入张量的某个维度为128, 则该维度必须保持为128.
5. 返回的函数要求在语法上完整且可运行, 不关注其他上下文依赖. 上述分析所得到的大小直接硬编码即可, 不需要额外分析调整
- 例如, 如果返回的函数依赖某个全局变量channel_size=128, 则可以直接在函数中使用该变量的值128.

任务成功判据: 不关注原始输入大小, 如果解析后发现生成的输入的大小在压缩后依旧在1024之上, 则任务失败, 否则将结果填入到json的"get_input_mini_code"字段中.
技巧: 
1. 对于batch size较大的输入,可以将batch size设置为1, 仅展示一个batch中的变换逻辑

任务2: 模型关键路径寻找
目标: 在不改变输入和模型结构的前提下, 寻找模型中输入到输出的关键变换路径.
要求:
1. 不需要对我提供的结构化python文件进行任何修改
2. 给出的索引必须合法
- 所有索引都必须在对应张量的实际形状范围内。
- 不允许给出超出范围的索引（例如超过维度大小或为负数的非法索引）。
3. 输出形式：两层列表
[
  [ ... 输入张量1的索引列表..., 输入张量2的索引列表..., ... ],
  [ ... 输出张量1的索引列表..., 输出张量2的索引列表..., ... ]
]
- 第一个列表中的每个元素，对应 get_inputs() 返回的每一个输入张量的关键路径索引；第二个列表中的每个元素，对应 Model forward 最终输出张量（或多个输出张量）的关键路径索引；
- 单个位置索引使用 (i) 表示，意为在该维上取一个具体的单点索引，例如 (0)。连续区间索引使用 (start, end) 表示, 等价于 Python 切片 start:end。
- 若某个模型只有 1 个输入张量、1 个输出张量，也必须用列表形式表示。

举例:
1. 3D卷积算子的关键路径中, 输出为y[d, h, w], 对应的关键路径输入为x[d : d+KD, h : h+KH, w : w+KW], 卷积核w[kd, kh, kw], 以列表形式返回为
[
    [[(d, d+KD), (h, h+KH), (w, w+KW)], [(kd), (kh), (kw)]],
    [[(d), (h), (w)]], 
]    
    其中, d,h,w,kd,kh,kw均为你通过模型分析得到的具体可行索引值.
2. layernorm算子这种在某维进行Reduction的算子, 假设y = layer_norm(x, gamma, beta).输入张量 x 的形状为 [B, L, D]，在最后一维 D 上做归一化；以列表形式返回为
[
    [[ (b0), (l0), (0, D) ]], # 输入 x：固定 batch & 位置，对最后一维 D 取全
    [[ (b0), (l0), (d0) ]]    # 输出 y：该单个元素的索引
]
技巧: 
1. 认为batch_size组成的维度不是关键路径的一部分, 可以固定该维度的索引为0.

任务成功判据: 不关注原始输入大小, 如果寻找到的关键路径所涉及的总的张量大小超过1024, 则任务失败, 否则将结果填入到json的"key_index_of_inouts"字段中.

任务3: 模型最小化
目标: 寻找可以展示模型关键变换的最小模型初始化参数和输入参数大小
要求:
1. 你可以改变模型的任意全局参数
2. 保持模型计算逻辑不变, 可以展示出较小的变换逻辑, 但是不能超过1024的输入大小限制
3. 将修改后的python代码完整的返回, 包括我提到的四个部分
任务成功判据: 任务一定能成功, 将结果填入到json的"minimized_model"字段中.


我的python文件内容如下:
{{ code_file_content }}

**请尽可能使用中文进行思考分析**

返回结果:
**请按照以下格式输出你的结果，仅返回json格式，不要在json外部有任何解释或补充说明：**
{
    "get_input_mini_code": "你的get_input_mini函数代码",
    "key_index_of_inouts": "原始输入和输出中关键变换路径的索引,以两个列表形式返回, 第一个列表代表所有输入的关键路径索引, 第二个列表代表所有输出的关键路径索引",
    "minimized_model": "你的最小化Model类代码, 形式与原始Model类相同",
    
}
其中, 三个字段的值根据你的分析只需要提供一个即可, 其他两个值置为空, 优先级根据顺序排列.